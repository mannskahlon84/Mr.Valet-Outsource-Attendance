fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.28.0,<1.0.0
sqlalchemy>=2.0.0,<3.0.0
psycopg2-binary>=2.9.9
geoalchemy2>=0.14.0
alembic>=1.13.0
python-jose[cryptography]>=3.3.0
pwdlib[argon2]>=0.2.0
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
python-multipart>=0.0.9
httpx>=0.27.0
numpy>=1.26.0,<3.0.0
requests>=2.31.0
openpyxl>=3.1.2
reportlab>=4.0.0
Pillow>=10.0.0
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Lightweight real face-matching for attendance selfies (see app/services/biometrics.py).
# dlib-bin ships a prebuilt wheel (manylinux x86_64, matches Render), so no C++ compiler or
# long build step is needed on deploy, unlike installing "dlib" itself from source.
dlib-bin>=20.0.0
# Bundles the pretrained model weights as ordinary package data (~96MB download, no
# separate model-download step at runtime). Its pkg_resources usage needs setuptools<81.
face_recognition_models==0.3.0
setuptools<81
